<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
        
    body
        h2 Reestablecer Password
        p Hola, has solicitado reestablecer tu password. Haz clic en el siguiente enlace para realizar la acción. Este enlace tiene caducidad, vuelve a solicitarlo si no funciona.
        a(href=`${resetUrl}`) Procede a resetear
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste este email, simplemente ignóralo